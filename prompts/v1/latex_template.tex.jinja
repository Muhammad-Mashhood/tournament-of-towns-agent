%% Tournament of Towns — Problem Set PDF Template
%% Generated by Tournament of Towns Archive Agent (v1)
%% Source: {{ source_url }}
%% DO NOT EDIT MANUALLY — regenerate via harness

\documentclass[12pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{geometry}
\usepackage{parskip}
\usepackage{enumitem}
\usepackage{microtype}
\usepackage{lmodern}
\usepackage{xcolor}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{hyperref}

\geometry{
  a4paper,
  top=2.5cm, bottom=3cm,
  left=2.5cm, right=2.5cm
}

\hypersetup{
  colorlinks=false,
  pdftitle={Tournament of Towns {{ tournament }}{{ round_label }} — {{ level_label }} {{ variant_label }}},
  pdfauthor={Tournament of Towns Archive Agent}
}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.4pt}
\fancyhead[L]{\small Tournament of Towns — {{ tournament_ordinal }} Tournament}
\fancyhead[R]{\small {{ level_label }}, {{ variant_label }}}
\fancyfoot[C]{\small Page \thepage{} of \pageref{LastPage}}
\fancyfoot[R]{\small\textcolor{gray}{Machine translation — not an official edition}}

\begin{document}

%% ─────────────────────────────────────────────────────────────
%%  HEADER BLOCK
%% ─────────────────────────────────────────────────────────────

\begin{center}
  {\LARGE\bfseries Tournament of Towns}\\[4pt]
  {\large\bfseries {{ tournament_ordinal }} Tournament, {{ academic_year }}}\\[8pt]
  {\large {{ round_label }} Round \quad\textbullet\quad {{ date_display }}}\\[4pt]
  {\large\bfseries {{ level_label }} \quad ({{ level_raw }})}\\[2pt]
  {\large\bfseries {{ variant_label }} variant \quad ({{ variant_raw }})}\\[8pt]
  \rule{\linewidth}{0.6pt}
\end{center}

\vspace{2pt}
\noindent\textit{{{ scoring_note }}}
\vspace{8pt}

%% ─────────────────────────────────────────────────────────────
%%  PROBLEMS
%% ─────────────────────────────────────────────────────────────

{% for problem in problems %}
\noindent\textbf{Problem {{ problem.number }}%
{% if problem.points %}\enspace[{{ problem.points }}~point{% if problem.points != 1 %}s{% endif %}]{% endif %}%
.}\enspace
{{ problem.text | latex_safe }}
{% if problem.author %}
\par\vspace{2pt}\hfill\textit{({{ problem.author }})}
{% endif %}
{% if problem.note %}
\par\vspace{2pt}\noindent\textcolor{red}{\small {{ problem.note }}}
{% endif %}
\vspace{12pt}

{% endfor %}

%% ─────────────────────────────────────────────────────────────
%%  FOOTER NOTE
%% ─────────────────────────────────────────────────────────────

\vfill
\rule{\linewidth}{0.4pt}\\[4pt]
\noindent\small
\textbf{Source:} \url{ {{ source_url }} }\\
\textbf{Translation:} Machine translation via {{ translation_model }}
(prompt version: {{ prompt_version }}).
This PDF was generated automatically for archival purposes.
It has not been officially reviewed by the Tournament of Towns committee.
Solutions are not included.

\end{document}
